<h2>Posts</h2>

<% @posts.each do |post| %>
  <article>
    <h3><%= post.title %></h3>

    <%= post.content %>
    <%= link_to "Edit", "/posts/#{post.id}/edit" %>
    <hr>
  </article>
<% end %>

<%= link_to "New Post", new_post_path %>
